# Cloudflare Workers config for the Study Hub API proxy.
# Free tier, global, and reachable from a school network.
name = "study-hub-api"
main = "worker.mjs"
compatibility_date = "2024-09-01"

# Set the Groq key as a secret instead of committing it:
#   npx wrangler secret put GROQ_API_KEY